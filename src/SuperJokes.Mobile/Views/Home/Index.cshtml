@{
    Layout = "~/Views/Shared/_BaseLayoutPage.cshtml";

    List<CategoryDto> categorylist = ViewBag.Category;
}
@model List<Joke.Model.Domain.T_Joke>
@using Joke.Model.Dto
@using SuperJokes.Mobile.Helpers
@foreach (var m in Model)
{
    <div class="haha">

        <h3><a href="@SuperJokeHelper.GetJokeDetail(m.ID)" target="_parent">@m.Title</a></h3>
        @Html.Raw(m.Content)
    </div>
}
<p id="pages"><a href="#">段子下→页</a></p>